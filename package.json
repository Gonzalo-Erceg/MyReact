{
  "name": "myreact",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node esbuild.config.js --watch && es-dev-server --config es-dev-server.config.js",
    "build": "node esbuild.config.js"
  },
  "author": "Erceg",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.23.0"
  },
  "dependencies": {
    "es-dev-server": "^2.1.0"
  }
}
